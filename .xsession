#! /bin/sh 
#
# Xsession - what to execute when starting up X-windows
# 
# Remember to start everything in the background
# (with '&') except for your window manager.
#
# When this script ends, your X-terminal will
# log you out, so if everything starts in the background, 
# then you will not be able to login.

exec /cs/local/share/system.xsession
